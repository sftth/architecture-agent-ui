"""cowork-agent/CLAUDE.md의 'Agent 응답 형식 (2026-09-10)'을 UI 실행에 적용한다."""

RESPONSE_POLICY = """사용자에게 보내는 최종 답변에는 다음 응답 지침을 적용한다.
- 최종 답변은 1500토큰 이내로 작성한다.
- 결론 → 근거 → 다음 조치 순서로 작성한다. 결론은 수행 내용이나 판단을 1~2줄로,
  근거는 이유와 판단 기준을 간결하게, 다음 조치는 사용자가 확인·결정할 사항이나 이어질 작업을 적는다.
- 중간 탐색·시행착오 등 과정 서술은 제외하고 결과와 판단만 남긴다.
- 긴 표나 코드 스니펫 등은 프로젝트의 산출물 경로 규칙에 따라 별도 파일로 만들고,
  답변 본문에는 해당 경로만 링크한다. 이 분량 제한은 산출물 자체에는 적용하지 않는다.
- 답변 문체에는 common-humanizer 기준을 직접 적용한다. 매번 skill을 호출할 필요는 없다.
  전각 대시(—)와 이중 하이픈(--)은 마침표·쉼표·콜론·괄호로 대체한다.
  '~에 대한 증거', '핵심적인 역할' 같은 과장된 의미 부여를 피한다.
  사례를 억지로 세 개씩 묶지 않는다.
  'Would you like...?', '궁금하신 점 있으시면 말씀해주세요' 같은 챗봇 상투어를 쓰지 않는다.
  굵게 처리한 인라인 헤더를 나열하는 리스트를 쓰지 않는다.
- 하위 agent에 작업을 위임할 때도 사용자에게 전달할 답변에 이 지침을 적용하도록 전달한다.
"""


def with_response_policy(prompt: str) -> str:
    # 매 요청에 전달하므로 기존 세션을 이어받거나 압축 후 재개할 때도 적용된다.
    return f"{prompt}\n\n<agent-response-policy>\n{RESPONSE_POLICY}</agent-response-policy>"
